%h1= @vehicle.name
%dl
  - [:color, :engine_volume, :mileage, :transmission, :release_year, :vin, :date_of_purchase, :mileage_on_purchase].each do |attr|
    - if @vehicle.send(attr)
      %dt= t("activerecord.attributes.vehicle.#{attr}")
      %dd= @vehicle.send(attr)
