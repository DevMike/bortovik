%section.contact
  = simple_form_for @contact_message, :url => contact_message_path do |f|
    %h2 Please fill in following form
    = f.input :name, :input_html => sender_input_attributes_for(:name, @contact_message)
    = f.input :email, :input_html => sender_input_attributes_for(:email, @contact_message)
    = f.input :subject
    = f.input :description, :as => :text
    = render 'shared/counter', :selector => '#contact_message_description'
    = f.submit "Submit"
    .clear