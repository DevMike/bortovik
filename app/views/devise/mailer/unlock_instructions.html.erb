<p><%= t('email.hello', who: @resource.email) %></p>

<p><%= t('devise.account.has_been_locked') %></p>

<p><%= t('devise.account.click_to_unlock') %></p>

<p><%= link_to t('devise.account.unlock_my_account'), unlock_url(@resource, :unlock_token => @resource.unlock_token) %></p>
